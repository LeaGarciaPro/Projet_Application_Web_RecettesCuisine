export interface Utilisateur {
    //jointure si on veut la recette où si depuis la recette on veut les avis
    nomRecette: String,
    //jointure si on veut ses infos
    pseudoUtilisateur: String,
    commentaire: String
}